#include<stdio.h>
void main(){
    int a[]={1,2,3,4,5,6,7,8,9,45,445,4,4,46,465,46,46,4,454,547,374,374,3,3},*ptr;

    // *******array sum using pointers
    // int sum=0;
    // for(ptr=&a[0];ptr<&a[(sizeof(a)/sizeof(a[0]))];ptr++){
    //     sum+=*ptr;
    // }
    // printf("The sum is %d",sum);

    // **********finding middle element
    // ptr=&a[(sizeof(a)/sizeof(a[0]))/2];
    // printf("Middle element is %d",*ptr);
}