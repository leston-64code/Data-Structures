#include<stdio.h>
void main(){
    // int x=10,*ptr=&x;
    // printf("Value of pointer is %d",*ptr);


    // we can also change the value stored in x using pointer
    // int x=10,*ptr=&x;
    // printf("Value stored in x after %d \n",*ptr);
    // *ptr=40;
    // printf("Value stored in x after %d ",*ptr);
}